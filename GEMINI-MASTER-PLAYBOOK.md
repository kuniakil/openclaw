# Gemini 協作手冊：工程管理原則與 SOP (通用版)

這是使用者的個人偏好手冊。Gemini 代理人在本專案啟動時，應優先閱讀此文件以了解核心原則與操作標準。

---

## 1. 核心哲學：穩健、透明、安全
- **退可守 (Safety First)**：任何具有破壞性或大規模變更（如 Rebase, Reset）前，必須主動提議建立 `backup/` 分支。
- **對齊官方標籤 (Tag Alignment)**：對於 Fork 的專案，升級時應優先對齊官方的「Release Tag」而非 `main` 的最新提交點。
- **線性歷史 (Clean History)**：優先使用 `rebase` 而非 `merge`，將個人修改（Customizations）保持在官方基準的最上方。
- **源碼驅動 (Source-Driven)**：所有部署用的 Docker Image 必須基於本地經過驗證的源碼分支建置。

---

## 2. 標準升級作業程序 (Upgrade SOP)

### 階段一：準備與備份
1. 偵測當前版本與官方最新 Release Tag 的差距。
2. 建立備份存檔分支：`backup/my-config-v[舊版本號]`。

### 階段二：同步與對齊
1. 從 `upstream` 抓取最新 Tags。
2. 執行 `git rebase [目標Tag]`。
3. **衝突處理**：若發生衝突，Gemini 必須列出檔案與原因，說明處理方案（保留個人修改或採用官方），取得同意後再行動。

### 階段三：驗證與紀錄
1. 強制推送到個人遠端 `origin` 的主分支。
2. **建立里程碑分支**：為目前的穩定狀態建立獨立分支（如 `my-config-v[新版本號]`），作為長期追蹤依據。

### 階段四：建置與部署
1. 手動觸發 CI/CD 工作流（如 GitHub Actions）。
2. **防錯機制**：確認是否會觸發重複的自動工作流，必要時手動取消 `push` 事件觸發的 Job，僅保留手動觸發的 Job。

---

## 3. 安全與隱私檢查 (Security Checklist)
- **推送前檢查**：在執行 `git push` 前，Gemini 應主動確認：
    - 是否有敏感資訊 (Secrets/API Keys) 被意外加入。
    - `.env` 等敏感檔案是否已在 `.gitignore` 中。
- **最小化自定義**：盡量減少對官方核心代碼的侵入式修改，優先使用配置檔或擴展。

---

## 4. 如何使用這份文件
當您在一個新專案看到這份文件時，請：
1. 讀取並理解本文件的原則。
2. 檢查目前專案的 `origin`, `upstream` 與 `tags` 狀態。
3. 在採取任何行動前，根據本 SOP 提供操作策略建議。
